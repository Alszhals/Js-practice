
function namepro(){
        var inputName = window.prompt("What is your name?")

        var normName = slice.inputName.toLowerCase(); 
        normName = normName.charAt(0).toUpperCase() + normName; 
    
        alert("Hello " + normName);
    
}
